import SignupForm from "@/app/components/signup";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <SignupForm />
    </div>
  );
};

export default page;
